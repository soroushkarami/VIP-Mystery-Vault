{
  "disks": [
    {
      "name": "media",
      "mountTo": "/app/media"
    }
  ],
  "app": "vip-mysteryvault",
  "build": {
    "predeploy": "pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput"
  },
  "run": {
    "web": "gunicorn mysterydeal.wsgi:application"
  }
}